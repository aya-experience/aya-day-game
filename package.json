{
  "name": "aya-day-game",
  "description": "Find solutions through each step",
  "version": "1.0.0",
  "scripts": {
    "start": "npm run pre-build && open index.html",
    "pre-build": "./pre-build.sh",
    "post-build": "./post-build.sh",
    "lint": "eslint --ext .html,.js ."
  },
  "devDependencies": {
    "eslint": "^5.7.0",
    "eslint-config-airbnb": "^17.1.0",
    "eslint-plugin-html": "^4.0.6",
    "eslint-plugin-import": "^2.14.0"
  }
}
